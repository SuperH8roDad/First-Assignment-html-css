# First-Assignment-html-css

<!-- 

Git Commands:
First time(creating a new repo):
git init
git add .
git branch -M main
git remote add origin <insert github link to repo here>
git commit -m "initial commit"
git push -u origin main

Future Commits(after creating repo):
git add .
git commit -m "descriptive message here"
git push

When working with others, before making any changes:
git pull origin main


Extra Homework:
1. finish the second html page and link the two pages together
2. style either a button or a link
3. resize image to fit screen
4. Add assignment instrctions to README.md file
 -->
